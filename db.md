-- This script was generated by the ERD tool in pgAdmin 4.
-- Please log an issue at https://github.com/pgadmin-org/pgadmin4/issues/new/choose if you find any bugs, including reproduction steps.
BEGIN;


CREATE TABLE IF NOT EXISTS public.account
(
    account_id uuid NOT NULL,
    customer_id uuid NOT NULL,
    account_number character varying(255) COLLATE pg_catalog."default" NOT NULL,
    currency character varying(3) COLLATE pg_catalog."default" NOT NULL,
    opening_balance numeric(18, 2) NOT NULL,
    balance numeric(18, 2) NOT NULL,
    account_status character varying(30) COLLATE pg_catalog."default" NOT NULL,
    version bigint NOT NULL DEFAULT 1,
    CONSTRAINT accounts_pkey PRIMARY KEY (account_id),
    CONSTRAINT account_account_number_key UNIQUE (account_number)
);

CREATE TABLE IF NOT EXISTS public.customer
(
    customer_id uuid NOT NULL,
    document_id character varying(255) COLLATE pg_catalog."default" NOT NULL,
    name character varying(255) COLLATE pg_catalog."default" NOT NULL,
    email character varying(255) COLLATE pg_catalog."default" NOT NULL,
    CONSTRAINT customer_pk PRIMARY KEY (customer_id),
    CONSTRAINT customer_document_id_key UNIQUE (document_id)
);

CREATE TABLE IF NOT EXISTS public.movement
(
    movement_id uuid NOT NULL,
    account_id uuid NOT NULL,
    transfer_id uuid,
    movement_type character varying(30) COLLATE pg_catalog."default" NOT NULL,
    amount numeric(18, 2) NOT NULL,
    previous_balance numeric(18, 2) NOT NULL,
    new_balance numeric(18, 2) NOT NULL,
    movement_description text COLLATE pg_catalog."default",
    created_at timestamp with time zone NOT NULL DEFAULT now(),
    CONSTRAINT movement_pk PRIMARY KEY (movement_id)
);

CREATE TABLE IF NOT EXISTS public.transfer
(
    transfer_id uuid NOT NULL,
    source_account_id uuid NOT NULL,
    target_account_id uuid NOT NULL,
    amount numeric(18, 2) NOT NULL,
    exchange_rate numeric(18, 8),
    converted_amount numeric(18, 2),
    idempotency_key uuid NOT NULL,
    created_at timestamp with time zone NOT NULL DEFAULT now(),
    transfer_status character varying(30) COLLATE pg_catalog."default" NOT NULL,
    CONSTRAINT transfer_pk PRIMARY KEY (transfer_id),
    CONSTRAINT transfer_idempotency_key_key UNIQUE (idempotency_key)
);

ALTER TABLE IF EXISTS public.account
    ADD CONSTRAINT fk_customer_id FOREIGN KEY (customer_id)
    REFERENCES public.customer (customer_id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE RESTRICT;


ALTER TABLE IF EXISTS public.movement
    ADD CONSTRAINT fk_movement_account FOREIGN KEY (account_id)
    REFERENCES public.account (account_id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE RESTRICT;


ALTER TABLE IF EXISTS public.movement
    ADD CONSTRAINT fk_movement_transfer FOREIGN KEY (transfer_id)
    REFERENCES public.transfer (transfer_id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE RESTRICT;


ALTER TABLE IF EXISTS public.transfer
    ADD CONSTRAINT fk_transfer_source_account FOREIGN KEY (source_account_id)
    REFERENCES public.account (account_id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;


ALTER TABLE IF EXISTS public.transfer
    ADD CONSTRAINT fk_transfer_target_account FOREIGN KEY (target_account_id)
    REFERENCES public.account (account_id) MATCH SIMPLE
    ON UPDATE NO ACTION
    ON DELETE NO ACTION;

END;